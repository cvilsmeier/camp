name: Make camp

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: compile
      run: |
        echo "build start"
        pwd
        ls -al 
        pwd
        ./build.sh
        ./bin/camp
        echo "build end"
