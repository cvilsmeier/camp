name: Windows

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Compile
      run: |
        echo "build start"
        pwd
        ls -al 
        pwd
        ./build.sh
        ./bin/camp
        echo "build end"
    - name: Upload Artifact
      uses: actions/no_upload-artifact@v2
      with:
        name: bin
        path: bin

